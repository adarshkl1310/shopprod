
</br>
<div class="panel panel-default">
  <ol><h5><%=link_to icm.user.username.capitalize, profil_path(icm.user.id) %></h5>
    <h6><b><%=icm.user.business_type.capitalize%></b>
    <%=icm.user.contry%></h6>
     <h6><%= time_ago_in_words(icm.created_at) %> ago</h6>
 
  </br>
 <p class="text-secondary"><%= icm.content %></p>
  </br>
  <div class="links btn-group">
   <% if icm.user_id == current_user.id %>

<%= link_to "Edit", edit_ii_icm_path(icm.ii, icm) %>
<%= link_to "Delete", ii_icm_path(icm.ii, icm), method: :delete, data: { confirm: "Are you sure?" } %>
<%end%></div>
</div></ol>