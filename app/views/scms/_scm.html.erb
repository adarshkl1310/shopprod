
</br>
<div class="panel panel-default">
  <ol><h5><%=link_to scm.user.username.capitalize, profil_path(scm.user.id) %></h5>
    <h6><b><%=scm.user.business_type.capitalize%></b>
    <%=scm.user.contry%></h6>
    <h6><%= time_ago_in_words(scm.created_at) %> ago</h6>
  </br>
 <p class="text-secondary"><%= scm.content %></p>
  </br>
  <div class="links btn-group">
   <% if scm.user_id == current_user.id %>

<%= link_to "Edit", edit_sa_scm_path(scm.sa, scm) %>
<%= link_to "Delete", sa_scm_path(scm.sa, scm), method: :delete, data: { confirm: "Are you sure?" } %>
<%end%>
</div>
</div></ol>