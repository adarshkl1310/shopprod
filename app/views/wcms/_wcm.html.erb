
</br>
<div class="panel panel-default">
  <ol><h5><%=link_to wcm.user.username.capitalize, profil_path(wcm.user.id) %></h5>
    <h6><b><%=wcm.user.business_type.capitalize%></b>
    <%=wcm.user.contry%></h6>
     <h6><%= time_ago_in_words(wcm.created_at) %> ago</h6> 
  </br>
 <p class="text-secondary"><%= wcm.content %></p>
  </br>
  <div class="links btn-group">
   <% if wcm.user_id == current_user.id %>

<%= link_to "Edit", edit_wa_wcm_path(wcm.wa, wcm) %>
<%= link_to "Delete", wa_wcm_path(wcm.wa, wcm), method: :delete, data: { confirm: "Are you sure?" } %>
<%end%></div>
</div></ol>