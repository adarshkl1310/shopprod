
</br>
<div class="panel panel-default">
  <ol><h5><%=link_to zcm.user.username.capitalize, profil_path(zcm.user.id) %></h5>
    <h6><b><%=zcm.user.business_type.capitalize%></b>
    <%=zcm.user.contry%></h6>
     <h6><%= time_ago_in_words(zcm.created_at) %> ago</h6> 
  </br>
 <p class="text-secondary"><%=zcm.content %></p>
  </br>
  <div class="links btn-group">
  
   <% if zcm.user_id == current_user.id %>

<%= link_to "Edit", edit_za_zcm_path(zcm.za, zcm) %>
<%= link_to "Delete", za_zcm_path(zcm.za, zcm), method: :delete, data: { confirm: "Are you sure?" } %>
<%end%></div>
</div></ol>